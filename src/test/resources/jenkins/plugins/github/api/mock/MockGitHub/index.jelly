<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler"><st:contentType value="application/json; charset=utf-8"/>{
  "current_user_url": "${it.url}/user",
  "current_user_authorizations_html_url": "https://github.com/settings/connections/applications{/client_id}",
  "authorizations_url": "${it.url}/authorizations",
  "code_search_url": "${it.url}/search/code?q={query}{&amp;page,per_page,sort,order}",
  "commit_search_url": "${it.url}/search/commits?q={query}{&amp;page,per_page,sort,order}",
  "emails_url": "${it.url}/user/emails",
  "emojis_url": "${it.url}/emojis",
  "events_url": "${it.url}/events",
  "feeds_url": "${it.url}/feeds",
  "followers_url": "${it.url}/user/followers",
  "following_url": "${it.url}/user/following{/target}",
  "gists_url": "${it.url}/gists{/gist_id}",
  "hub_url": "${it.url}/hub",
  "issue_search_url": "${it.url}/search/issues?q={query}{&amp;page,per_page,sort,order}",
  "issues_url": "${it.url}/issues",
  "keys_url": "${it.url}/user/keys",
  "notifications_url": "${it.url}/notifications",
  "organization_repositories_url": "${it.url}/orgs/{org}/repos{?type,page,per_page,sort}",
  "organization_url": "${it.url}/orgs/{org}",
  "public_gists_url": "${it.url}/gists/public",
  "rate_limit_url": "${it.url}/rate_limit",
  "repository_url": "${it.url}/repos/{owner}/{repo}",
  "repository_search_url": "${it.url}/search/repositories?q={query}{&amp;page,per_page,sort,order}",
  "current_user_repositories_url": "${it.url}/user/repos{?type,page,per_page,sort}",
  "starred_url": "${it.url}/user/starred{/owner}{/repo}",
  "starred_gists_url": "${it.url}/gists/starred",
  "team_url": "${it.url}/teams",
  "user_url": "${it.url}/users/{user}",
  "user_organizations_url": "${it.url}/user/orgs",
  "user_repositories_url": "${it.url}/users/{user}/repos{?type,page,per_page,sort}",
  "user_search_url": "${it.url}/search/users?q={query}{&amp;page,per_page,sort,order}"
}</j:jelly>
